<h1><%= @flat.name %></h1>

<div>
  <p><%= @flat.description %>
  <p>Address: <%= @flat.address %>
  <p>Price per night: $<%= @flat.price_per_night %>
  <p>Capacity: <%= "🧍‍♂️" * @flat.number_of_guests %></p>
</div>

<p>
  <%= link_to "Home", flats_path, class: "btn btn-primary text-decoration-none"%>
</p>

<p>
  <%= link_to "Update", edit_flat_path, class: "btn btn-primary text-decoration-none"%>
</p>

<p>
  <%= link_to "Delete",
  flat_path(@flat),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"},
  class: "btn btn-danger text-decoration-none"
  %>
</p>
